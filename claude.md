# 项目协作约定

## 架构边界

- podcast_collector 负责 RSS 采集、来源留存和知识卡生成。
- feishu_bot 负责飞书消息交互，只读知识卡数据库，不修改采集结果。
- 用户的收藏和最近抽取状态单独写入 data/feishu_bot.sqlite3。
- 所有 App ID、App Secret 和 API 密钥只从环境变量或本地 .env 读取。
- 当前机器人使用飞书 WebSocket 长连接，本机运行时不需要公网回调地址。
- 没有来源的 AI 自动总结不得直接发布成知识卡。

## 当前 MVP

- 单用户、本机常驻。
- 支持有内容暗示的每日话题、选号和骰子随机抽取。
- 通过飞书互动卡片展示骰子滚动、概念揭晓与收藏操作。
- 每次抽取生成新的知识卡消息，保留此前抽到的卡片，不覆盖话题入口或旧卡。
- 支持“收藏”“取消收藏”和“我的收藏”的文字后备命令。
- 每天为同一个飞书会话生成稳定的六个候选概念。
- 回复包含概念解释、学习价值、不确定性、关联概念和播客出处。
- 暂不做语音、多用户账号、支付或自动发布。

## 已接受的取舍

- 第一版复用数据库中已有知识卡，不依赖 OpenAI 密钥。
- 先验证“话题暗示—骰子揭晓—收藏”是否让用户愿意继续学习，再增加语音和个性化推荐。
